import {Add} from "./Add";
import {RequestBox} from "./RequestBox";

export const Requests = () => {
    return(
        <div className="container">
            <Add/>
            <RequestBox/>
        </div>
    );
}
